# OpenSpec Configuration — wrenchify
# Auto-generated by SDD init on 2026-08-25

project:
  name: wrenchify
  description: Auto repair shop management application
  version: 0.1.0
  license: MIT

stack:
  desktop:
    framework: electron-or-tauri  # Decision pending
    runtime: desktop
  frontend:
    framework: react-or-vue-or-svelte  # Decision pending
    language: typescript
  database:
    engine: sqlite
    mode: single-file
    location: local

constraints:
  offline_first: true
  bilingual:
    languages:
      - it  # Italian
      - es  # Spanish
    strategy: i18n
  single_user: true
  data_portability: single-file-database

testing:
  strict_tdd: false
  reason: No test frameworks detected in greenfield project
  frameworks: []
  test_command: null
  coverage_command: null
  layers:
    unit: false
    integration: false
    e2e: false

sdd:
  mode: hybrid  # Both OpenSpec files and Engram persistence
  delivery_strategy: ask-on-risk
  review_budget_lines: 400
  execution_mode: interactive
